# Heavy, CI-only dependencies for the CNN front-detection ingestion pipeline.
# NOT installed on the Space (see server/requirements.txt for the runtime deps).
# Install alongside server/requirements.txt in the ingest GitHub Action.

# CPU-only torch keeps the install small/fast on the runner.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4,<3

# ERA5 access (ARCO zarr on public GCS) + array stack.
xarray>=2024.6
zarr>=3.0,<4
gcsfs>=2024.6
numpy>=2.0
scipy>=1.11

# Vectorisation (skeletonize).
scikit-image>=0.24

# Dataset pull/push + weights download.
huggingface_hub>=0.25,<2.0
httpx>=0.27
certifi
